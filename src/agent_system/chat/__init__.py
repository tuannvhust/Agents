"""Chainlit chat UI for the Agent System.

Run with:
    chainlit run src/agent_system/chat/app.py -w

Environment variables are read from the same .env file as the FastAPI app.
Ensure the agent-postgres (and optionally Redis) services are reachable.
"""
